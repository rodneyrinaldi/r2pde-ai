project:
  name: Corporate Agenda
  root: scaffold_done
  steps:
    - type: mkdir
      path: .r2pde-ai
    - type: cli
      command: npm init -y
      comment: Inicializa o npm (cria package.json).
    - type: git-init


    # Essenciais
