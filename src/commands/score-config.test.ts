import { describe, it, expect, vi } from 'vitest';
import * as scoreConfigCommand from './score-config.js';

describe('score-config command', () => {
  it('should be defined', () => {
    expect(scoreConfigCommand).toBeDefined();
  });

  // Adicione mais testes específicos para a lógica do comando aqui
});
