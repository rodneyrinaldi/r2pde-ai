  ] });
  // Compose config object from prompt results
  const config: PdeConfig = {
    ...DEFAULT_CONFIG,
    version: '0.1.0',
    language,
    artifactsLanguage,
    git: { ...DEFAULT_CONFIG.git },
    score: { ...DEFAULT_CONFIG.score },
    ai: { ...DEFAULT_CONFIG.ai },
    project: {
      type: projectType,
      architecture,
      maturity,
      hasAuth,
      hasPayment,
      hasIntegrations,
      audience,
      stack,
    },
  };
      hasAuth: answers.hasAuth,
      hasPayment: answers.hasPayment,
      hasIntegrations: answers.hasIntegrations,
      audience: answers.audience,
      stack: answers.stack,
    },
  };
  saveConfig(cwd, config);

